<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Name="pagemain"
    x:Class="LTgarlic.Views.MainHomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LTgarlic.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <StackPanel>
        <TextBlock Text="Welcome to LTgarlic" Style="{StaticResource PageTitleStyle}"/>
        <TextBlock Text="v0.1" Style="{StaticResource CaptionTextBlockStyle}"/>

        <TextBlock Text="recent Projects: " Style="{StaticResource BaseTextBlockStyle}" Margin="0,50,0,0"/>

        <TextBlock Text="looks like you haven't open a project in a wile..." Style="{StaticResource CaptionTextBlockStyle}" Margin="0,10,0,0"/>
        <RelativePanel Margin="0,30,0,0">
            <Button x:Name="createbutton" Style="{StaticResource AccentButtonStyle}" Content="Create a new File" Margin="0,0,0,0" Click="createbutton_Click"/>
            <Button x:Name="openbutton" Style="{StaticResource ButtonRevealStyle}" Content="Open a File" Margin="20,0,0,0" RelativePanel.RightOf="createbutton" Click="openbutton_Click"/>
            <Button x:Name="importlbrbutton" Style="{StaticResource AccentButtonStyle}" Content="Open a Libary" Margin="20,0,0,0" RelativePanel.RightOf="openbutton" Click="importlbrbutton_Click"/>
        </RelativePanel>

        <Canvas Height="{Binding ElementName=pagemain, Path=ActualHeight, UpdateSourceTrigger=PropertyChanged}" Width="{Binding ElementName=pagemain, Path=ActualWidth, UpdateSourceTrigger=PropertyChanged}">
            <Line 
                X1="0" Y1="{Binding ElementName=pagemain, Path=ActualHeight, UpdateSourceTrigger=PropertyChanged}"
                X2="700" Y2="300"
                Stroke="{ThemeResource AccentTextFillColorTertiaryBrush }"
                StrokeThickness="5"
                />        
        </Canvas>
   
        
    </StackPanel>
</Page>
